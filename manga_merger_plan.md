# خطة تطوير أداة دمج المانجا الجديدة (Manga PSD Merger GUI) وتحويلها إلى EXE

سنقوم ببناء أداة جديدة تجمع بين واجهة ويب رسومية (GUI) متطورة وسرعة تشغيل فائقة عبر خيوط المعالجة المتوازية (Parallel Workers). كما سنقوم بحزم التطبيق بالكامل ليصبح ملفاً تنفيذياً واحداً (`.exe`) يحمل أيقونة مخصصة (رمز) لسهولة التشغيل.

---

## 1. هيكل المشروع الجديد (Project Structure)
سنقوم بإنشاء الملفات والمجلدات التالية داخل المجلد الرئيسي للأداة:
* **`package.json`**: إدارة الاعتمادات والسكربتات.
* **`server.js`**: خادم الويب الخلفي (Express + API + WebSockets).
* **`worker.js`**: معالج الصفحات المتوازي باستخدام `Worker Threads` و `sharp` و `ag-psd`.
* **`build.js`**: سكربت تجميع وحزم التطبيق كملف `.exe` وتثبيت الأيقونة.
* **`app_icon.png`**: الأيقونة الرسمية للتطبيق التي قمنا بإنشائها.
* **`public/`**: واجهة المستخدم الأمامية (HTML / CSS / JS):
  * `index.html`: هيكل الواجهة.
  * `style.css`: التصميم الأنيق (Dark Theme + Glassmorphism).
  * `app.js`: منطق التفاعل والاتصال بالسيرفر.

---

## 2. كيفية حزم التطبيق وتحويله إلى EXE بأيقونة مخصصة

لتحويل المشروع إلى ملف `.exe` واحد مع أيقونة مخصصة دون حدوث مشاكل عطب في الملف، سنتبع الخطوات التالية:

### الخطوة أ: تحويل الأيقونة إلى صيغة ICO
سنستخدم مكتبة `png-to-ico` أو كود تحويل مدمج في سكربت البناء لتحويل `app_icon.png` إلى أيقونة متكاملة الأحجام `app_icon.ico` (تحتوي على مقاسات 16x16, 32x32, 64x64, 128x128, 256x256).

### الخطوة ب: استخدام مكتبة `pkg` للتجميع
مكتبة `@vercel/pkg` هي الأداة القياسية لتحويل كود Node.js إلى ملف `.exe`. سنقوم بتهيئة ملف `package.json` لتوجيه `pkg` لتضمين ملفات واجهة المستخدم الأمامية (الموجودة في مجلد `public`) داخل ملف الـ EXE نفسه كـ `assets`.

### الخطوة ج: تعديل الأيقونة والبيانات الوصفية
بعد قيام `pkg` بإنشاء ملف `manga-psd-merger-gui.exe` الأولي، سنقوم بتشغيل سكربت `build.js` الذي يستخدم مكتبة **`resedit`** (وهي مكتبة JavaScript نقية 100% لتعديل الملفات التنفيذية على ويندوز). هذا السكربت سيقوم بـ:
1. فتح ملف الـ EXE الناتج.
2. استبدال الأيقونة الافتراضية بأيقونتنا الجديدة `app_icon.ico`.
3. تعديل معلومات الملف (مثل اسم البرنامج، الشركة المصنعة، ورقم الإصدار).
4. حفظ الملف النهائي الجاهز للتشغيل.

---

## 3. خطة العمل بالتفصيل (Work Tasks)

### [ ] المهمة 1: إعداد أيقونة التطبيق الرسمية
* كتابة سكربت لتحويل `app_icon.png` إلى ملف أيقونة متوافق `app_icon.ico`.
* التحقق من سلامة الأيقونة.

### [ ] المهمة 2: بناء واجهة المستخدم الخلفية (Backend)
* إنشاء `package.json` مع كافة الاعتمادات المطلوبة.
* بناء السيرفر الرئيسي `server.js` مع واجهات الـ API لتصفح المجلدات وفحص الفصول.
* إنشاء ملف الـ `worker.js` لمعالجة الصور وبناء ملفات الـ PSD بالتوازي.

### [ ] المهمة 3: بناء واجهة المستخدم الأمامية (Frontend GUI)
* تصميم `index.html` مع مستكشف الملفات وشاشة المعاينة.
* كتابة كود CSS العصري (`style.css`) بمظهر داكن جذاب وتأثيرات زجاجية.
* كتابة كود التفاعل `app.js` للربط بـ WebSockets لمتابعة التقدم حياً.

### [ ] المهمة 4: كتابة سكربت الحزم والبناء `build.js`
* برمجة عملية التجميع التلقائي.
* دمج الأيقونة وتحديث معلومات الـ EXE.

### [ ] المهمة 5: التحقق والتجربة
* تشغيل ملف الـ EXE الناتج وتجربته بالكامل للتأكد من خلوه من الأخطاء ودمج الفصول بنجاح.
* تسليم الأداة النهائية الجاهزة للاستخدام.
